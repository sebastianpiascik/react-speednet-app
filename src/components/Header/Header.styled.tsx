import styled from 'styled-components';

const StyledWrapper = styled.div`
  display: flex;
  align-items: center;
`;

const StyledLogo = styled.img`
  height: 50px;
`;

export { StyledLogo, StyledWrapper };
